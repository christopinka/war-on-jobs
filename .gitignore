__pycache__/
*.pyc
.venv/
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/
.DS_Store
*.log

# Local data files
# Keep curated source CSVs in Git so the repo stays self-contained.
# Generated runtime artifacts are excluded unless intentionally checked in.
/data/backups/
/data/screened/
/data/logs/
/data/*.jsonl
*.jsonl

# Optional local runtime state
*.tmp
